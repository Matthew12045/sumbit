# Shepherd workspace internals (untracked runtime state, like .git)
.vcscore/

# Python bytecode / caches
__pycache__/
*.pyc
.pytest_cache/

# Bot secrets — never commit the real .env
.env
# Pre-deepseek switch backup of .env (contains the gateway.9arm.co token)
.env.qwen-backup

# Debug log output
meeting_bot_debug.log
